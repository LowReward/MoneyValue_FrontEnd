import { createStore } from 'vuex'

export default createStore({
  state: {
    user:{
      data:{},
      // Une fois le token reçu, token passera de null à son token
      token: null 
    }},
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})

